<UserControl
    x:Class="Magehelper.Views.Controls.AdditionalGlyphControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:Magehelper.ViewModels.Controls"
    d:DataContext="{d:DesignInstance Type=viewModel:AdditionalGlyphControlViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="40" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Text}" />
        <Button Grid.Column="1" Command="{Binding DeleteCommand}">
            <Image
                Width="15"
                Height="15"
                Source="/Assets/del_button.png" />
        </Button>
    </Grid>
</UserControl>
