<UserControl
    x:Class="Magehelper.Views.Controls.ArcaneGlyphControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:CommunityToolkit.WinUI.UI.Controls;assembly=CommunityToolkit.WinUI.UI.Controls.Primitives"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:Magehelper.ViewModels.Controls"
    d:DataContext="{d:DesignInstance Type=viewModel:ArcaneGlyphControlViewModel}"
    mc:Ignorable="d">
    <StackPanel>
        <controls:DockPanel
            Margin="5"
            HorizontalAlignment="Stretch"
            LastChildFill="True">
            <TextBlock controls:DockPanel.Dock="Left" Text="{Binding Name}" />
            <Button
                controls:DockPanel.Dock="Right"
                Command="{Binding ReactivateCommand}"
                Content="Reaktivieren"
                IsEnabled="{Binding IsReactivateButtonEnabled}"
                Visibility="{Binding IsReactivateButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
        </controls:DockPanel>
        <TextBlock Margin="5" Text="{Binding Duration}" />
        <TextBlock Margin="5" Text="{Binding AppliedTo}" />
        <StackPanel>
            <ItemsControl Margin="5" ItemsSource="{Binding AdditionalGlyphs}" />
        </StackPanel>
        <TextBlock Margin="5" Text="{Binding Size}" />
        <TextBlock Margin="5" Text="{Binding Rkw}" />
        <TextBlock Margin="5" Text="{Binding Rkp}" />
        <TextBlock Margin="5" Text="{Binding Cost}" />
    </StackPanel>
</UserControl>
