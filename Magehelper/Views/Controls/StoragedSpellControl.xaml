<UserControl
    x:Class="Magehelper.Views.Controls.StoragedSpellControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:Magehelper.ViewModels.Controls"
    d:DataContext="{d:DesignInstance Type=viewModel:StoragedSpellControlViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Path=Text}" />
            <Button Grid.Column="1" Command="{Binding RemoveSpellCommand}">
                <Image Source="/Assets/del_button.png" />
            </Button>
        </Grid>
    </Grid>
</UserControl>
