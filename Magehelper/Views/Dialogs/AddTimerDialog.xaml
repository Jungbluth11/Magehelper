<ContentDialog
    x:Class="Magehelper.Views.Dialogs.AddTimerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:Magehelper.ViewModels.Dialogs"
    Title="Timer hinzufügen"
    CloseButtonText="Abbrechen"
    PrimaryButtonCommand="{Binding SubmitCommand}"
    PrimaryButtonText="Hinzufügen"
    Style="{ThemeResource DefaultContentDialogStyle}">
    <ContentDialog.DataContext>
        <vm:AddTimerDialogViewModel />
    </ContentDialog.DataContext>
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Text="Name" />
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Margin="5"
            Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <ComboBox
            Grid.Row="1"
            Grid.Column="0"
            Margin="5"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            ItemsSource="{Binding DurationUnits}"
            SelectedIndex="0"
            SelectedValue="{Binding DurationUnit, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <NumberBox
            Grid.Row="1"
            Grid.Column="1"
            KeyDown="UIElement_OnKeyDown"
            Minimum="0"
            SpinButtonPlacementMode="Compact"
            Value="{Binding DurationValue, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
    </Grid>
</ContentDialog>
