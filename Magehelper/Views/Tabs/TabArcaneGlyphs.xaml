<TabViewItem
    x:Class="Magehelper.Views.Tabs.TabArcaneGlyphs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Magehelper.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:Magehelper.ViewModels.Tabs"
    xmlns:winUiControls="clr-namespace:CommunityToolkit.WinUI.UI.Controls;assembly=CommunityToolkit.WinUI.UI.Controls.Layout"
    Header="Zauberzeichen"
    IsClosable="False"
    mc:Ignorable="d">
    <TabViewItem.DataContext>
        <viewModel:TabArcaneGlyphsViewModel />
    </TabViewItem.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Orientation="Horizontal">
            <Button
                Margin="5"
                Click="BtnAddArcaneGlyph_OnClick"
                Content="Zauberzeichen hinzufügen" />
            <Button
                Margin="5"
                Command="{Binding ToNextDayCommand}"
                Content="einen Tag weiter" />
        </StackPanel>
        <ListView
            Grid.Row="1"
            Grid.Column="0"
            Height="500"
            ItemsSource="{Binding ArcaneGlyphs}"
            SelectionChanged="Selector_OnSelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:ArcaneGlyphListItemControl DataContext="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <controls:ArcaneGlyphControl
            Grid.Row="1"
            Grid.Column="1"
            HorizontalAlignment="Stretch"
            DataContext="{Binding CurrentArcaneGlyph}" />
    </Grid>
</TabViewItem>
