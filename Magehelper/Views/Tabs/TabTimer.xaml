<TabViewItem
    x:Class="Magehelper.Views.Tabs.TabTimer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Magehelper.ViewModels.Tabs"
    x:Name="RootTabTimer"
    Header="Timer"
    IsClosable="False"
    mc:Ignorable="d">
    <TabViewItem.DataContext>
        <vm:TabTimerViewModel />
    </TabViewItem.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button
            Grid.Row="0"
            Grid.Column="0"
            Margin="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Click="BtnAddTimer_Click"
            Content="Timer hinzufügen" />
        <ItemsControl
            Grid.Row="1"
            Grid.Column="0"
            ItemsSource="{Binding TimersLeft}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Margin="5" Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Text}" />
                        <TextBlock Grid.Column="1" Text="{Binding DurationString}" />
                        <Button
                            Grid.Column="2"
                            Command="{Binding DataContext.DecreaseKrCommand, ElementName=RootTabTimer}"
                            CommandParameter="{Binding}"
                            Content="eine KR weiter" />
                        <Button
                            Grid.Column="3"
                            Margin="5,0,0,0"
                            Command="{Binding DataContext.RemoveTimerCommand, ElementName=RootTabTimer}"
                            CommandParameter="{Binding}">
                            <Image Source="/Assets/del_button.png" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ItemsControl
            Grid.Row="1"
            Grid.Column="1"
            ItemsSource="{Binding TimersRight}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Margin="5" Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Text}" />
                        <Button
                            Grid.Column="2"
                            Command="{Binding DataContext.DecreaseKrCommand, ElementName=RootTabTimer}"
                            CommandParameter="{Binding}"
                            Content="eine KR weiter" />
                        <Button
                            Grid.Column="3"
                            Margin="5,0,0,0"
                            Command="{Binding DataContext.RemoveTimerCommand, ElementName=RootTabTimer}"
                            CommandParameter="{Binding}">
                            <Image Source="/Assets/del_button.png" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</TabViewItem>
